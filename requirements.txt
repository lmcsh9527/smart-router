# TokenSaver 网关运行依赖（Python ≥ 3.10）
# 推荐：bash install.sh （自动创建 .venv 并安装本清单）

fastapi>=0.128
uvicorn>=0.39
httpx>=0.28

# ── 智能分类器（vendor/opensquilla）运行依赖 ──
numpy>=2.0
scikit-learn>=1.5
lightgbm>=4.5
onnxruntime>=1.20
tokenizers>=0.20
structlog>=24.0
joblib>=1.4
PyYAML>=6.0

# 可选：sentence-transformers（仅惰性的训练/嵌入回退路径用，
# 网关运行与判档不需要——生产环境未装亦正常）
